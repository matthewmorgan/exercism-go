// Package reverse has a method that reverses a string
package reverse

import "github.com/golang/example/stringutil"

// method String takes a string as inpput and returns it reversed
func String(input string) string {
	return stringutil.Reverse(input)
}
